Todo 3 evidence: deterministic generated resume snapshots
Base: 7eb226029cb93d7a5d3ce3022fc3b2d0c74a2d76
Date/as-of used for deterministic QA: 2026-07-12 UTC

Scope delivered
- sync-resume-data CLI precedence: --as-of, RESUME_AS_OF, one captured current UTC date.
- Strict YYYY-MM-DD UTC calendar parsing rejects rollover dates.
- runSync accepts injected asOf/sourceDir/outputDir/sourceManifest and throws errors.
- The manifest is exactly ko/en/ja and outputs exactly data.json, data_en.json, data_ja.json.
- Copied canonical sources use resume_schema.json; both generated runs use validatePortfolioData.
- Corresponding generated bytes must match before the verifier removes only its owned temp child.
- Output writes use O_NOFOLLOW descriptors; unowned/symlink-marker temp roots are refused.
- build owns one sync:data then raw build:worker; automation delegates through build once.
- auto-sync workflow has contents: read, npm ci, and npm run verify:ssot only.
- No release workflow, lockfile, canonical source, or generated snapshot is tracked or changed.

TDD evidence
1. Baseline characterization was added and run before production edits.
   Command: node --test tools/scripts/utils/__tests__/verify-resume-sync.test.js
   Initial result: 1 baseline test passed; current source-to-three-output filenames and observable experience derivation pinned.
2. Failing-first contracts were then added before production edits.
   Command: node --test tools/scripts/utils/__tests__/verify-resume-sync.test.js
   Pre-fix result: exit 1; baseline 1 passed and all 8 new contracts failed for missing strict parser/injected paths/verifier/scripts.
3. Baseline remains as resume-sync-baseline.test.js after the test-size split.
   Final command: node --test tools/scripts/utils/__tests__/verify-resume-sync.test.js tools/scripts/utils/__tests__/resume-sync-baseline.test.js tools/scripts/utils/__tests__/resume-sync-output-safety.test.js
   Final result: 13 tests, 13 passed, 0 failed.
4. Exact failure-fixture command:
   node --test --test-name-pattern='rejects invalid source|rejects invalid generated portfolio' tools/scripts/utils/__tests__/verify-resume-sync.test.js
   Result: 2 tests executed, 2 passed, 0 failed.

Automated verification
- npm run verify:ssot: PASS; two source-valid, portfolio-valid generations matched and owned temp root was absent afterward.
- RESUME_AS_OF=2026-07-12 npm run build: PASS; sync marker preceded "Starting improved worker generation".
- timeout 600s env RESUME_AS_OF=2026-07-12 npm run automate:ssot: exit 0; exactly 1 root sync marker; worker line 55 before first test gate line 104.
- timeout 900s env RESUME_AS_OF=2026-07-12 npm run automate:full: exit 0; exactly 1 root sync marker; worker line 60 before first Jest gate line 117; Jest 116/116 suites passed.
- npm run test:tools: 50/50 passed before the final three safety assertions; final focused aggregate is 13/13.
- npx eslint <all changed JS/MJS tests>: PASS with no warnings.
- npm run typecheck: PASS.
- npx prettier --check <changed files>: PASS.
- git diff --check: PASS.
- git ls-files 'apps/portfolio/data*.json' 'apps/portfolio/worker.js': empty.
- auto-sync inspection: contents: read; no pull-requests/write permission, create-pull-request, git diff, or write path; only app command is npm run verify:ssot.

Automation environment note
- The first automate:full attempt exposed a detached-worktree dependency overlay issue: @resume/shared resolved to the main worktree, creating duplicate module identities in browser-index.test.js.
- Hypothesis 1 (Todo 3 regression) was refuted because the same source passed with task-local workspace links.
- Hypothesis 2 (random Jest flake) was refuted because the focused test failed repeatedly with the wrong link.
- Hypothesis 3 (workspace link identity) was confirmed: changing only @resume workspace links toggled the focused test from fail to pass; no source fix was made.
- The corrected current dependency overlay produced the recorded green automate:full result. Fresh npm ci/lock convergence remains Todo 6 ownership.

Manual CLI/data QA
- Copied only resume_data.json, resume_data_en.json, resume_data_ja.json to a task-owned temp source directory.
- Ran twice:
  node tools/scripts/utils/sync-resume-data.js --as-of 2026-07-12 --source-dir <copied-sources> --output-dir <run1|run2>
- Both commands exited 0 and each output directory contained exactly the three required files.
- validatePortfolioData accepted all 6 generated documents.
- Corresponding byte comparisons passed 3/3.
- SHA-256 run hashes:
  data.json    b4534eb42dd3ec942804a8e3c56c3bf19a05752261a7e60b2c97d614906311f0
  data_en.json 8d82a6ca9611218fc1f9a7feeee9b6477c864a375f24e88ca25576999f7777a4
  data_ja.json deeae9698eec109d5801eb786239597ae614d5e6967299a6fff9b1dbfa350bde
- Invalid --as-of 2026-02-30 exited 1, created no invalid output directory, and canonical SHA-256 values stayed unchanged.
- RESUME_AS_OF=2026-07-12 npm run build emitted the fixed sync marker, worker build marker, and a generated worker containing the health surface; ignored outputs were removed afterward.

UltraQA (all nine)
1. malformed_input: PASS - invalid calendar, missing source.personal, invalid generated {}, output symlink, and marker symlink all rejected.
2. stale_state: PASS - two clean fixed-date runs matched; verifier removes its marked stale child but refuses unowned state.
3. dirty_worktree: PASS - prior generated files are byte-restored by tests; canonical sources and unrelated tracked files are untouched.
4. hung_or_long_commands: PASS - automate:ssot/full used 600s/900s bounds and exited 0.
5. flaky_tests: PASS - focused suite repeated green; the detached-worktree identity issue was isolated and corrected without source changes.
6. misleading_success_output: PASS - asserted file count, Zod validation, SHA/byte equality, nonzero invalid exit, marker count, and log ordering.
7. repeated_interruptions: PASS - owned marker cleanup handles verifier residue; unowned directories are preserved; stray reviewer automation was terminated and artifacts removed.
8. prompt_injection: N/A - inputs are structured local JSON and CLI flags; no untrusted instruction channel is consumed.
9. cancel_resume: N/A - no product cancellation/resume protocol exists; long commands were bounded rather than resumed.

Post-implementation review
- Goal/constraints: PASS.
- Hands-on QA: PASS.
- Code quality: PASS.
- Context/ownership mining: PASS.
- File safety: PASS after O_NOFOLLOW output/marker descriptor remediation; focused safety 3/3.

Cleanup and residual risk
- Removed task-owned .tmp verifier/manual roots, ignored data*.json and worker.js, dependency overlay, temp logs, and stray automation processes.
- Canonical source diff is empty; no generated files are tracked.
- Residual risk: simultaneous in-process verifyResumeSync calls sharing the same tempBase are unsupported; the specified GitHub workflow serializes runs with concurrency.

Independent needs-fix remediation (follow-up)
- Base commit: 08554f25a355ee01dd2d6d2742cf9a2d0ec2f759.
- TDD red command:
  node --test --test-name-pattern='preserves a victim swapped|rejects an output directory below' tools/scripts/utils/__tests__/resume-sync-output-safety.test.js
- Pre-fix result: exit 1, tests 2, pass 0, fail 2.
  - Swapped victim failed with ENOENT because the verifier recursively deleted the replacement directory.
  - Symlink-ancestor output failed with Missing expected exception and escaped into the external directory.
- Temp remediation: the verifier creates a unique run-* directory with mkdtemp under the fixed verify-resume-sync parent. Cleanup unlinks only the exact known source/output filenames and uses non-recursive rmdir only for empty generated directories; a swapped-in victim is preserved without any validation-to-recursive-delete window.
- Pre-existing files and symlinks beneath the fixed parent remain untouched; no caller-selected or pre-existing root is recursively removed.
- Output remediation: every existing output-directory path component is lstat-checked before directory creation and again before writes. Symlinked outputDir or ancestor traversal is rejected; final output files retain O_NOFOLLOW descriptor writes.
- Size remediation: resume-sync-runner.js was split by responsibility into resume-sync-derivations.js and resume-sync-output.js. Physical LOC: runner 99, derivations 82, output 45.

Follow-up verification
- Focused baseline/contracts/file-safety: 15/15 passed before the two final static/manual probes; safety suite includes swapped victim and symlink ancestor.
- Exact negative source/output filter: 2/2 passed.
- npm run test:tools: 53/53 passed.
- npm run verify:ssot: passed; unique run child removed.
- RESUME_AS_OF=2026-07-12 npm run build: passed with exactly the sync and worker markers.
- Changed-file ESLint: passed with no warnings.
- Changed-file Prettier check: passed.
- npm run typecheck: passed.
- git diff --check: passed.
- gitleaks detect --source . --config .gitleaks.toml --redact --no-banner: 1906 commits and approximately 395 MB scanned; no leaks found.
- automate:ssot/full were not rerun because this follow-up did not change package automation ordering; the independent verifier's exit-0 one-marker/order evidence at 08554f25 remains applicable.

Follow-up manual and adversarial QA
- Fixed-date CLI ran twice against copied canonical sources and separate outputs: exact three files per run, 6/6 portfolio schema validations, 3/3 byte-identical pairs.
- SHA-256 values remained b4534eb4... (ko), 8d82a6ca... (en), deeae969... (ja).
- Invalid 2026-02-30 exited 1, created no output, and canonical source hashes remained unchanged.
- malformed_input: invalid date/source/generated shape, final-file symlink, and output-directory ancestor symlink rejected.
- stale_state: pre-existing parent content and linked entry survived; unique child cleaned; swapped victim survived.
- dirty_worktree: canonical source diff remained empty and ignored build outputs were cleanup-only.
- misleading_success_output: exact file inventory, schemas, byte comparisons, exit codes, marker count, and hashes were asserted rather than inferred from success text.
- cleanup: only task-owned /tmp and unique run directories, ignored portfolio outputs, and verification logs are removed.

Directory-FD binding remediation (second follow-up)
- Base commit: 7980f506add4faf91ee92b990bdeccd066aef972.
- TDD red command:
  node --test --test-name-pattern='contract-shaped victim|immediately before the first output open' tools/scripts/utils/__tests__/resume-sync-output-safety.test.js
- Pre-fix result: exit 1, tests 2, pass 0, fail 2.
  - Contract-shaped swapped victim failed with ENOENT because pathname cleanup deleted its known data.json and directory tree.
  - First-output-open directory swap failed with Missing expected exception because the mutable ancestor redirected snapshots externally.
- Added secure-directory.js as the fail-closed Linux directory-FD boundary. It requires O_DIRECTORY, O_NOFOLLOW, and /proc/self/fd, validates the opened directory identity, and exposes only leaf-relative paths.
- Verifier creation opens and retains FDs for the unique root plus sources/run-1/run-2. Exact cleanup unlink/rmdir operations use /proc/self/fd/<dirfd>/... paths. The root pathname is removed only when opened fstat identity still equals current lstat; on mismatch the current victim is untouched and displaced unique residue is intentionally retained.
- Output generation opens and pins each output directory before generation. All three leaf opens use the pinned /proc/self/fd path plus O_NOFOLLOW. The original output path identity is rechecked before success; mismatch rejects and removes written snapshots only through the pinned directory handle.
- Platform gate test removes /proc/self/fd availability and proves runSync rejects before output directory creation.
- Physical LOC remains below 200: secure-directory 71, resume-sync-output 98, resume-sync-runner 111, verifier 125, safety test 184.

Directory-FD follow-up verification
- Cumulative focused baseline/contracts/safety/platform: 18/18 passed.
- Exact two race regressions: 2/2 passed.
- Exact invalid source/generated-output filter: 2/2 passed.
- npm run test:tools: 56/56 passed.
- npm run verify:ssot: passed with no unique-child residue.
- RESUME_AS_OF=2026-07-12 npm run build: passed with sync and worker markers.
- Changed-file ESLint and Prettier: passed.
- npm run typecheck and git diff --check: passed.
- gitleaks: 1907 commits and approximately 395.14 MB scanned; no leaks found.
- automate:ssot/full were not rerun because package automation ordering was untouched.
- Manual fixed-date runs: exact three files each, 6/6 schema validations, 3/3 byte pairs, unchanged hashes b4534eb4... / 8d82a6ca... / deeae969....
- Manual invalid 2026-02-30: exit 1, no output directory, canonical source hashes unchanged.
- Cleanup probes: contract-shaped current victim/root survived; displaced output snapshots were removed via pinned FD; external sentinel directory received zero snapshots.
