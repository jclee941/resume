{
  "name": "@resume/job-dashboard-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Job automation dashboard Cloudflare Worker",
  "dependencies": {
    "@cloudflare/puppeteer": "^1.4.0",
    "@resume/env": "*",
    "@resume/schemas": "*",
    "@resume/shared": "*",
    "@resume/types": "*"
  },
  "scripts": {
    "deploy": "node -e \"console.error('Standalone job-dashboard deploy disabled per ADR 0009 (merged into apps/portfolio). Use git push to master.'); process.exit(1)\""
  }
}
