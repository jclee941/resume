/**
 * @deprecated Import from '@resume/shared/ua' instead.
 * Re-export stub for backward compatibility.
 */
export {
  DEFAULT_USER_AGENT,
  CHROME_USER_AGENTS as USER_AGENT_POOL,
  getRandomUA as getRandomUserAgent,
} from '@resume/shared/ua';
