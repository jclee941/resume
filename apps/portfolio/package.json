{
  "name": "@resume/portfolio-worker",
  "version": "1.0.0",
  "description": "Edge-deployed portfolio (resume.jclee.me)",
  "private": true,
  "dependencies": {
    "@resume/schemas": "*",
    "@resume/shared": "*",
    "@resume/types": "*"
  },
  "scripts": {
    "build": "node generate-worker.js",
    "start": "CLOUDFLARE_ENV= wrangler dev --config ../../wrangler.jsonc"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0"
  }
}
